FILE=TABLE
PRODUCT=buildmanager

Group:
  Flavor=NULL
  Qualifiers=""

Common:
  Action=Setup
    setupRequired("expect v5_21")
    envSet(BUILDMANAGER_DIR, ${UPS_PROD_DIR})
    envPrepend(PATH, ${UPS_PROD_DIR}/bin)

  Action=UnSetup
    envUnset(BUILDMANAGER_DIR)
    envRemove(PATH, ${UPS_PROD_DIR}/bin)
    unsetupRequired("expect")

  Action=Current

  Action=UnCurrent

  Action=Configure

  Action=UnConfigure
END:

